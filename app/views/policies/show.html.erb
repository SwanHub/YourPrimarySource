<h1>Policy Title: <%=@policy.title%></h1>
<h2>Category: <%=@policy.category%></h2>
<h3>Description: <%=@policy.description%></h3>

<h2><% @policy.candidates.each do |candidate|%>
    Candidate: <%= link_to candidate.name, candidate_path(candidate)%>
<% end %></h2>

<h3>Posts: <% @policy.posts.each do |post|%></h3>
  <p>Author: <%= link_to post.user.full_name, user_path(post.user) %></p>
  <p>Title: <%= link_to post.title, post_path(post) %></p>
  <p>Content: <%= post.content%></p>
<% end %>

<%= link_to "All Policies", policies_path%>
